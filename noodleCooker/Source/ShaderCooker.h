#pragma once
#include "Core/NoodleCore.h"

class ShaderCooker
{
public:
	bool CookShaderByteCode(const std::string& sourcePath, const std::string& entry, const std::string& profile, const std::string& outputPath);

protected:
private:
};
